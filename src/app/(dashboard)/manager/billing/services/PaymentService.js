import { collection, doc, runTransaction, serverTimestamp } from "firebase/firestore";
import { db } from "@/lib/firebase";
import { allocatePayment } from "./PaymentAllocationService";
class PaymentService {async create(companyId,input,user){const paymentRef=doc(collection(db,"Companies",companyId,"Payments"));const invoiceRef=doc(db,"Companies",companyId,"Invoices",input.invoiceId);return runTransaction(db,async(transaction)=>{const snapshot=await transaction.get(invoiceRef);if(!snapshot.exists())throw new Error("Invoice not found.");const invoice={id:snapshot.id,...snapshot.data()};if(["cancelled","draft"].includes(String(invoice.status).toLowerCase()))throw new Error("Payments cannot be recorded against draft or cancelled invoices.");const allocation=allocatePayment(invoice,input.amount);if(!allocation.valid)throw new Error(allocation.message);const payment={id:paymentRef.id,companyId,invoiceId:invoice.id,invoiceNumber:invoice.invoiceNumber,projectId:invoice.projectId,projectName:invoice.projectName,clientId:invoice.clientId,clientName:invoice.clientName,amount:Number(input.amount),paymentDate:input.paymentDate,mode:input.mode,referenceNumber:input.referenceNumber||"",transactionId:input.transactionId||"",notes:input.notes||"",status:"received",receivedBy:{id:user?.id||user?.uid||"",name:user?.name||user?.displayName||"Finance"},createdAt:serverTimestamp(),updatedAt:serverTimestamp()};transaction.set(paymentRef,payment);transaction.update(invoiceRef,{paidAmount:allocation.paid,pendingAmount:allocation.pending,status:allocation.status,paymentDate:input.paymentDate,updatedAt:serverTimestamp()});return payment;});}}
export default new PaymentService();
export { allocatePayment } from "./PaymentAllocationService";
